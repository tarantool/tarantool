name: packaging

on:
  push:
    branches:
      - 'master'
      - 'release/**'
      - 'foxzi/static_build_make_source_packages'
    tags:
      - '**'
  pull_request:
    types: [ opened, reopened, synchronize, labeled ]
  workflow_dispatch:
  workflow_call:
    inputs:
      submodule:
        description: Name of submodule to bump.
        required: true
        type: string
      revision:
        description: Git revision from submodule repository
        required: true
        type: string

concurrency:
  # Update of a developer branch cancels the previously scheduled workflow
  # run for this branch. However, the 'master' branch, release branch, and
  # tag workflow runs are never canceled.
  #
  # We use a trick here: define the concurrency group as 'workflow run ID' +
  # 'workflow run attempt' because it is a unique combination for any run.
  # So it effectively discards grouping.
  #
  # Important: we cannot use `github.sha` as a unique identifier because
  # pushing a tag may cancel a run that works on a branch push event.
  group: ${{ (
    github.ref == 'refs/heads/master' ||
    startsWith(github.ref, 'refs/heads/release/') ||
    startsWith(github.ref, 'refs/tags/')) &&
    format('{0}-{1}', github.run_id, github.run_attempt) ||
    format('{0}-{1}', github.workflow, github.ref) }}
  cancel-in-progress: true

jobs:
  packaging-deb-x86_64:
    # Run on push to the 'master' and release branches of tarantool/tarantool
    # or on pull request if the 'full-ci' or 'static-build-ci' label is set.
    if: github.repository == 'tarantool/tarantool' &&
        ( github.event_name != 'pull_request' ||
          contains(github.event.pull_request.labels.*.name, 'full-ci') ||
          contains(github.event.pull_request.labels.*.name, 'static-build-ci') )

    uses: ./.github/workflows/static_build_pack_test_deploy_deb.yml
    with:
      arch: x86_64
      test-matrix: '{
        "include": [
          {"os": "debian-buster"}, {"os": "debian-bullseye"},
          {"os": "ubuntu-focal"}, {"os": "ubuntu-jammy"}
        ]
      }'
      submodule: ${{ inputs.submodule }}
      revision: ${{ inputs.revision }}
      testlib-path: /usr/lib/tarantool-testlib/obj-x86_64-linux-gnu/
    secrets: inherit

  packaging-rpm-x86_64:
    # Run on push to the 'master' and release branches of tarantool/tarantool
    # or on pull request if the 'full-ci' or 'static-build-ci' label is set.
    if: github.repository == 'tarantool/tarantool' &&
        ( github.event_name != 'pull_request' ||
          contains(github.event.pull_request.labels.*.name, 'full-ci') ||
          contains(github.event.pull_request.labels.*.name, 'static-build-ci') )

    uses: ./.github/workflows/static_build_pack_test_deploy_rpm.yml
    with:
      arch: x86_64
      test-matrix: '{
        "include": [
          {"os": "centos-7"}, {"os": "centos-8"},
          {"os": "fedora-35"}, {"os": "fedora-36"},
        ]
      }'
      submodule: ${{ inputs.submodule }}
      revision: ${{ inputs.revision }}
      testlib-path: /usr/lib64/tarantool-testlib/
    secrets: inherit

  packaging-deb-aarch64:
    # Run on push to the 'master' and release branches of tarantool/tarantool
    # or on pull request if the 'full-ci' or 'static-build-ci' label is set.
    if: github.repository == 'tarantool/tarantool' &&
        ( github.event_name != 'pull_request' ||
          contains(github.event.pull_request.labels.*.name, 'full-ci') ||
          contains(github.event.pull_request.labels.*.name, 'static-build-ci') )

    uses: ./.github/workflows/static_build_pack_test_deploy_deb.yml
    with:
      arch: aarch64
      test-matrix: '{
        "include": [
          {"os": "debian-buster"}, {"os": "debian-bullseye"},
          {"os": "ubuntu-focal"}, {"os": "ubuntu-jammy"}
        ]
      }'
      testlib-path: /usr/lib/tarantool-testlib/obj-aarch64-linux-gnu/
    secrets: inherit

  packaging-rpm-aarch64:
    # Run on push to the 'master' and release branches of tarantool/tarantool
    # or on pull request if the 'full-ci' or 'static-build-ci' label is set.
    if: github.repository == 'tarantool/tarantool' &&
        ( github.event_name != 'pull_request' ||
          contains(github.event.pull_request.labels.*.name, 'full-ci') ||
          contains(github.event.pull_request.labels.*.name, 'static-build-ci') )

    uses: ./.github/workflows/static_build_pack_test_deploy_rpm.yml
    with:
      arch: aarch64
      test-matrix: '{
        "include": [
          {"os": "centos-7"}, {"os": "centos-8"},
          {"os": "fedora-35"}, {"os": "fedora-36"}
        ]
      }'
      testlib-path: /usr/lib64/tarantool-testlib/
    secrets: inherit
