-- Options for test-run tarantoolctl

local workdir = os.getenv('TEST_WORKDIR')
default_cfg = {
    pid_file   = workdir,
    wal_dir    = workdir,
    memtx_dir  = workdir,
    vinyl_dir  = workdir,
    log        = workdir,
    background = false,
}

instance_dir = workdir

-- vim: set ft=lua :
