-- Options for test-run tarantoolctl

local workdir = os.getenv('TEST_WORKDIR')
default_cfg = {
    pid_file   = workdir,
    wal_dir    = workdir,
    snap_dir   = workdir,
    phia_dir   = workdir,
    logger     = workdir,
    background = false,
}

instance_dir = workdir

-- vim: set ft=lua :
