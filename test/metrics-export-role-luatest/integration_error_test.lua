require('test.metrics-export-role-luatest.helper')()

-- The test needs to be reworked so that it reads the standard error
-- from the log file. The test currently fails.
-- t.assert_error(require(
--     'third_party.metrics-export-role.test.integration.error_test'))
