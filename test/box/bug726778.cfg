#
# Used in args.test
#
slab_alloc_arena = 0.1

pid_file = "box.pid"

work_dir = "bug726778"
snap_dir = "snapshots"
wal_dir = "xlogs"

logger="cat - >> tarantool.log"

primary_port = 33013
secondary_port = 33014
admin_port = 33015

rows_per_wal = 50
