[default]
core = app
description = tarantool multiengine tap tests
is_parallel = True
use_unix_sockets_iproto = True
config = engine.cfg
lua_libs = lib/tester.lua
fragile = {
    "retries": 1,
    "tests": {
    }
  }
