## feature/box

* Introduced a new `grant` privilege that allows non-owners and non-admin
  users to grant and revoke accesses to an object or object class the access
  is granted for to other available users (gh-11528).
