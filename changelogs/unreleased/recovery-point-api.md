## feature/box

* Added `box.backup.recovery_point.create()` to create a recovery point
  (gh-11729).
* Added the `recovery_points` field to the `box.backup.info()` result, showing
  recovery points located in backup xlogs (gh-11729).
