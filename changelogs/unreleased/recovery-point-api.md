## feature/box

* Added `box.backup.recovery_point.create()` to create recovery point
  (gh-11729).
* Added `recovery_point` field to the `box.backup.info()` result showing
  recovery points lying in backup xlogs (gh-11729).
