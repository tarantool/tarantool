## feature/metrics

* Added support for exporting metrics over HTTP using the bundled
  `metrics-export-role` role. The role can be configured via the
  declarative `metrics.export` or `roles.metrics-export` (gh-xxx).
